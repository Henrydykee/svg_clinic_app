name: savouge
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'


dependency_overrides:
  meta: 1.16.0
  analyzer: ^7.3.0

dependencies:
  flutter:
    sdk: flutter
  http: 1.3.0
  scoped_model: 2.0.0
  provider: 6.1.1
  gap: 3.0.1
  flutter_secure_storage: 9.2.4
  dio: ^5.8.0+1
  get_it: 8.0.3
  package_info_plus: 8.2.1
  device_info_plus: 11.3.0
  connectivity_plus: 6.1.3
  flutter_svg: 2.0.17
  pin_code_fields: 8.0.1
  flutter_switch: 0.3.2
  logger: 2.5.0
  local_auth: 2.3.0
  shared_preferences: 2.5.2
  dartz: 0.10.1
  device_manager: 0.0.2
  overlay_support: 2.1.0
  flutter_pin_code_fields: 2.2.0
  cached_network_image: 3.4.1
  fluttertoast: 8.2.12
  smooth_page_indicator: 1.2.1
  firebase_remote_config: 5.4.1
  firebase_core: 3.12.0
  qr_code_scanner: 1.0.1
  image_picker: ^1.1.2
  image: 4.5.3
  intl: 0.19.0
  percent_indicator: 4.2.4
  currency_text_input_formatter: 2.2.8
  webview_flutter: ^4.9.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dart_mappable: ^4.4.0
  dart_mappable_builder: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.14

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/images/


  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
fonts:
  - family: Sora
    fonts:
      - asset: assets/font/Sora-Thin.ttf
        weight: 100
      - asset: assets/font/Sora-ExtraLight.ttf
        weight: 200
      - asset: assets/font/Sora-Light.ttf
        weight: 300
      - asset: assets/font/Sora-Regular.ttf
        weight: 400
      - asset: assets/font/Sora-SemiBold.ttf
        weight: 600
      - asset: assets/font/Sora-Bold.ttf
        weight: 700
      - asset: assets/font/Sora-ExtraBold.ttf
        weight: 800
